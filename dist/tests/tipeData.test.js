"use strict";
describe('tipeData', function () {
    it('should must declare', function () {
        let name = 'aisyah';
        let age = 28;
        let student = false;
        console.log(name);
        console.log(age);
        console.log(student);
    });
});
